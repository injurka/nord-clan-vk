import styled from '@emotion/styled';

export const MainStyled = styled.main`
  position: relative;
  width: 100%;
  z-index: 10;
  padding-top: var(--header-height);

  min-height: 100vh;
  height: 100%;
`;
